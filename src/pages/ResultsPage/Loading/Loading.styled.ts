import styled from 'styled-components';

export const Container = styled.div`
  height: max-content;
  max-width: 400px;
  position: fixed;
  bottom: 50%;
  right: 50%;
  transform: translate(50%, 50%);
  z-index: 5;
  padding: 25px;
  background: white;
  border-radius: 3px;
`;
